import { Meteor } from "meteor/meteor";
import { Players } from "./../imports/api/players";

Meteor.startup(() => {
  // Players.insert({
  //   name: "Luciana",
  //   score: 18
  // });
  // console.log(Players.find().fetch());
  // let numbers = [9, 99, 56, 67];
  // console.log(numbers);
  // let newNumbers = numbers.map(number => number + 1);
  // console.log(newNumbers);
});
